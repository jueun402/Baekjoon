'''
사다리 게임 

N개의 세로선 M개의 가로선 
N = 5, H = 6


str.lower => 알파벳 앞의 값 기준 정렬 
list(zip('abcde', [1,2,3,4,5]))

집합1.difference(집합2) # 차집합 
집합1.discard(7)

집합1.intersection(집합2) # 교집합 

같은 원소의 개수 
len(집합1.intersection(집합2))

'''